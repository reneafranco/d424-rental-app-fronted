/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface UploadEquipmentCoverPicture$Params {
  'equipment-id': number;
      body?: {
'file': Blob;
}
}

export function uploadEquipmentCoverPicture(http: HttpClient, rootUrl: string, params: UploadEquipmentCoverPicture$Params, context?: HttpContext): Observable<StrictHttpResponse<{
}>> {
  const rb = new RequestBuilder(rootUrl, uploadEquipmentCoverPicture.PATH, 'post');
  if (params) {
    rb.path('equipment-id', params['equipment-id'], {});
    rb.body(params.body, 'multipart/form-data');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<{
      }>;
    })
  );
}

uploadEquipmentCoverPicture.PATH = '/equipments/cover/{equipment-id}';
